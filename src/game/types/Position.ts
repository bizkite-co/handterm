export interface SpritePosition {
    leftX: number,
    topY: number
}

export interface FramePostion {
    leftX: number,
    topY: number
}

export interface Motion {
    dx: number,
    dy: number
}