export const TERMINAL_CONSTANTS = {
  PROMPT: '> ',
  PROMPT_LENGTH: 2, // Length of '> '
} as const;

// UI element sizing — tweak these to perfect element dimensions
export const UI_SIZES = {
  CHORD_SVG: {
    width: 75,
    height: 59.516,
  },
} as const;
